"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { useSearchParams } from "next/navigation"; import { api } from "@/lib/api";
export default function ResetPasswordPage(){const params=useSearchParams();const token=params.get("token")??"";const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [done,setDone]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");if(password!==confirm){setError("Passwords do not match.");return;}try{await api.post("/auth/reset-password",{token,password});setDone(true);}catch{setError("This reset link is invalid or expired.");}}return <main className="flex min-h-screen items-center justify-center bg-surface px-6"><section className="w-full max-w-md rounded-2xl bg-surface-container-lowest p-7 shadow-sm"><h1 className="text-headline-lg font-bold">Set a new password</h1>{done?<><p className="mt-4 text-body-md text-on-surface-variant">Your password has been changed. You can now sign in.</p><Link href="/login" className="mt-6 block text-center font-semibold text-primary">Go to sign in</Link></>:<form onSubmit={submit} className="mt-6 flex flex-col gap-4"><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="h-12 rounded-xl border border-outline-variant bg-surface px-3"/><input required minLength={8} type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm new password" className="h-12 rounded-xl border border-outline-variant bg-surface px-3"/><p className="text-label-sm text-on-surface-variant">Use 8–128 characters with uppercase, lowercase, and a number.</p><button className="h-12 rounded-xl bg-primary font-bold text-on-primary">Reset password</button>{error&&<p className="text-body-sm text-red-700">{error}</p>}</form>}</section></main>}
