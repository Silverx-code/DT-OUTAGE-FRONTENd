"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { api } from "@/lib/api";
export default function LoginPage() { const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [show,setShow]=useState(false); const [error,setError]=useState(""); const [busy,setBusy]=useState(false); async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const result=await api.post<{token:string}>("/auth/login",{email,password});sessionStorage.setItem("gridline_token",result.token);window.location.href="/dashboard";}catch{setError("Invalid email or password.");}finally{setBusy(false);}} return <main className="flex min-h-screen items-center justify-center bg-surface px-6 py-10"><div className="w-full max-w-md"><div className="mb-8 flex items-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-primary text-on-primary shadow-sm"><span className="text-2xl font-bold">G</span></div><div><p className="text-label-sm font-bold uppercase tracking-[0.2em] text-primary">Gridline</p><p className="text-body-sm text-on-surface-variant">DT Outage Operations</p></div></div><section className="rounded-2xl bg-surface-container-lowest p-7 shadow-sm"><p className="text-label-sm font-bold uppercase tracking-widest text-primary">Secure workspace</p><h1 className="mt-2 text-headline-lg font-bold">Sign in to Gridline</h1><form onSubmit={submit} className="mt-7 flex flex-col gap-4"><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-12 rounded-xl border border-outline-variant bg-surface px-3"/><div className="relative"><input required type={show?"text":"password"} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="h-12 w-full rounded-xl border border-outline-variant bg-surface px-3 pr-20"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-label-sm font-semibold text-primary">{show?"Hide":"Show"}</button></div><button disabled={busy} className="h-12 rounded-xl bg-primary font-bold text-on-primary disabled:opacity-50">{busy?"Signing in…":"Sign in"}</button></form>{error&&<p className="mt-4 rounded-lg bg-error-container p-3 text-label-sm text-on-error-container">{error}</p>}<Link href="/forgot-password" className="mt-5 block text-center text-label-md font-semibold text-primary underline">Forgot your password?</Link></section><p className="mt-6 text-center text-label-sm text-on-surface-variant">Authorized operations personnel only</p></div></main>; }
