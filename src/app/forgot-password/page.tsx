"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { api } from "@/lib/api";
export default function ForgotPasswordPage(){const [email,setEmail]=useState("");const [sent,setSent]=useState(false);async function submit(e:FormEvent){e.preventDefault();await api.post("/auth/forgot-password",{email}).catch(()=>undefined);setSent(true);}return <main className="flex min-h-screen items-center justify-center bg-surface px-6"><section className="w-full max-w-md rounded-2xl bg-surface-container-lowest p-7 shadow-sm"><h1 className="text-headline-lg font-bold">Reset your password</h1>{sent?<p className="mt-4 text-body-md text-on-surface-variant">If an account matches that email, you&apos;ll receive a reset link shortly.</p>:<form onSubmit={submit} className="mt-6 flex flex-col gap-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-12 rounded-xl border border-outline-variant bg-surface px-3"/><button className="h-12 rounded-xl bg-primary font-bold text-on-primary">Send reset link</button></form>}<Link href="/login" className="mt-6 block text-center font-semibold text-primary">Back to sign in</Link></section></main>}
